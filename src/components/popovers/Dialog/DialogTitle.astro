---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<h2 class="dialog-title" {id}>
  <slot />
</h2>

<style>
  .dialog-title {
    margin: 0;
    font-size: 18px;
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.4;
  }
</style>
